.container {
  display: flex;
  align-items: center;
  flex-direction: column;
  flex-grow: 1;
}

h1 {
  font-style: bold;
  color: white;
  text-transform: uppercase;
  font-size: 60;
  background: gray;
}

.banner {
  padding: 50px
}

body.welcome {
  background-image: url(<%= asset_path 'beer_background.jpg' %>);
  background-color: #cccccc;
  background-repeat: no-repeat;
}

.links {
  display: flex;
  justify-content: flex-start'
}

.nav {
  display: flex;
  justify-content: flex-end;
  flex-direction: rows;
  list-style-type: none;
}

.nav li {
  font-size: 20px;
  padding-right: 15px;
}
